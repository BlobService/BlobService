namespace BlobService.Core.Security
{
    public enum BlobAccessTypes
    {
        Read,
        Write,
        Delete
    }

    public enum ContainerAccessTypes
    {
        Read,
        Write,
        Delete,
        List
    }
}
